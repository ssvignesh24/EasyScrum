/** @format */

import React, { useContext } from "react";
import ReactDOM from "react-dom";

export default function ({ children }) {
  return <div className="w-full h-full text-center">Not found</div>;
}
